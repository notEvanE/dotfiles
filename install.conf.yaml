- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.doom.d:
    # ~/.test:

- create:
    - ~/inbox/tranfer
    # - ~/downloads
    # - ~/.vim/undo-history

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
