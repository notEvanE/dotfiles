- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.doom.d:
    ~/.oh-my-zsh/custom: ~/.dotfiles/oh-my-zsh
    ~/.p10k.zsh:
    # ~/.test:

- create:
    - ~/inbox/tranfer
    # - ~/downloads
    # - ~/.vim/undo-history

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
