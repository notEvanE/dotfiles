- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#     ~/.vim: vim
    ~/.zshrc: zshrc

- create:
    # - ~/downloads
    # - ~/.vim/undo-history

# - shell:
#   - [git submodule update --init --recursive, Installing submodules]
